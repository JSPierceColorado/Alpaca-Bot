gspread==6.2.1
oauth2client==4.1.3
requests==2.31.0

selenium==4.20.0
webdriver-manager==4.0.1

# These packages make ChromeDriver work smoothly in containers:
# (They are installed via Dockerfile, but leaving them here for clarity)
# chromium-chromedriver
# chromium
